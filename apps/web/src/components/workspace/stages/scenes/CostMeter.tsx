'use client';

// STUB — implemented in Sub-phase G (cost meter wiring)
// Renders nothing for now; real implementation will show running USD spend.

interface Props {
  projectId: string;
}

export function CostMeter(_: Props) {
  return null;
}
