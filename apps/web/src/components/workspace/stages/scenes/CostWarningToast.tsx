'use client';

// STUB — implemented in Sub-phase G (cost meter wiring)
// Renders nothing for now; real implementation will pop a toast when the
// project crosses MANGO_COST_WARN_THRESHOLD_USD.

interface Props {
  projectId: string;
}

export function CostWarningToast(_: Props) {
  return null;
}
