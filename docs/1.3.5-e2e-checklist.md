# Phase 1.3.5 — E2E Manual Checklist

**Status:** awaiting live session with maintainer.

Per [feedback_e2e_with_user_before_merge](memory:feedback_e2e_with_user_before_merge) rule, this walkthrough must complete BEFORE merge to main. Static checks (typecheck + biome + vitest + Next build) cannot detect Director chat-tool hallucinations, version-array regressions, polling/Realtime breaks, or storage-mirror failures; only a live walkthrough can.

## Pre-conditions

- [ ] Branch `claude/phase-1.3.5` checked out локально или Vercel preview deployed.
- [ ] Migrations applied на staging Supabase (`mgsfjyojbidhkxiknhsy`):
  - [ ] `phase_1_3_5_versioned_scenes` — DDL для `media_jobs.kind` enum.
  - [ ] Node script: `pnpm tsx scripts/migrate-phase-1.3.5.ts --env=staging` (jsonb upgrade).
- [ ] `scene-assets` bucket exists в staging.
- [ ] ENV variables в local `.env.local` или Vercel preview env:
  - [ ] `FAL_KEY` — real key.
  - [ ] `OPENROUTER_API_KEY` — real.
  - [ ] `MANGO_DEFAULT_NARRATOR_VOICE_ID=21m00Tcm4TlvDq8ikWAM`.
  - [ ] `MANGO_COST_WARN_THRESHOLD_USD=10` and/or `NEXT_PUBLIC_COST_WARN_THRESHOLD_USD=10`.
  - [ ] `MEDIA_PROVIDER=fal`, `LLM_PROVIDER=openrouter`.
  - [ ] `STORAGE_PROVIDER=fal_cdn` (Phase 1.3.5 mirror keeps fal CDN as primary, Supabase async).
- [ ] `pnpm dev` (or preview build) starts without errors.

## 1. Stage 04 inline (storyboard refactor)

- [ ] Открыть существующий проект (создан в 1.3, дошёл до Stage 03+).
- [ ] Stage 04 «Сцены» рендерится **inline** в Workspace, без CTA-кнопки и без redirect.
- [ ] Direct GET `/projects/[id]/storyboard` возвращает 404 (route удалён).
- [ ] Layout: vertical-list, каждая сцена = grid `168px thumb + side panel`.
- [ ] Стили из `storyboard-inline.css` применились (mango-glass card).

## 2. First-frame versions

- [ ] Click 🖼️ Кадр на сцене с пустыми version arrays → spinner overlay → fal job → версия v1 appended → preview.
- [ ] Repeat 4 раза (regen с разными промптами через modal — см. секцию 4) → versions v1..v5, v5 active.
- [ ] 6-я регенерация → v1 dropped (counter `5/5`), новая версия = active.
- [ ] Click dot v3 → preview switches к v3, появляется кнопка `↺ откат на пред.`.
- [ ] Click `↺ откат` → active обновляется на ту, что по `generated_at` идёт перед текущей.

## 3. Video versions

- [ ] Click 🎬 Видео → submit использует **active** first_frame как ref.
- [ ] Verify в `media_jobs.request_input` что image_url = active first_frame storage URL.
- [ ] Repeat 4 раза → versions v1..v5; 6-я → v1 dropped.
- [ ] Mode toggle 🖼️ Кадр / 🎬 Видео в нижнем оверлее thumb переключает что показано в preview.

## 4. Prompt editor modal

- [ ] Click ✏️ открыть на «Промпт первого кадра» → modal opens с current prompt'ом.
- [ ] Edit text → click `▶ Применить и regen` → modal closes → новая версия appended с edited prompt'ом в `version.prompt`.
- [ ] Esc / click backdrop → modal closes без save.
- [ ] Word counter в modal-meta обновляется при редактировании.

## 5. Per-card tier toggle

- [ ] Project tier = Премиум (TopBar). Сцена без `config_overrides.tier` → pill показывает 💎 Премиум.
- [ ] Click pill → переключение на 🪙 Эконом → `script.scenes[i].config_overrides.tier='economy'`.
- [ ] Если у сцены был premium-only `config_overrides.model` — toast/return value «модель X reverted to default Y».
- [ ] Verify в DB через Supabase SQL editor: `script.scenes[i].config_overrides.tier`.

## 6. Audio mode auto-detect

- [ ] Сцена с `dialogue.text='Привет!'`, `audio_mode='auto'`, premium model (Seedance 2.0 Pro / Veo 3.1) → resolveAudioMode возвращает `silent_tts`.
- [ ] Generate video → silent video в результате (без native audio dub'а).
- [ ] Generate voice TTS (через Director: «озвучь сцену 1») → ElevenLabs job → voice_audio v1 appended с правильным `voice_id`.
- [ ] Compose final clip (Director: «собери финальный клип сцены 1») → ffmpeg merge → `scene.final_clip.composed_from = { video_version_id, voice_audio_version_id }`.
- [ ] Сцена с `dialogue.text='Hello world'`, native model → resolveAudioMode = `native`.
- [ ] Generate video → native audio в результате; final_clip auto-set = active video (no mux).

## 7. Per-card audio_mode override

- [ ] Click `🤖 auto` pill → cycles `native → silent_tts → auto`.
- [ ] Override применяется при следующем submit (verify через `script.scenes[i].audio_mode`).

## 8. Voice picker в Stage 02 character card

- [ ] Open character card (Stage 02) → новый control `🗣️ Голос: [select]`.
- [ ] Dropdown содержит 6 voices из VOICE_POOL (Rachel, Adam, Domi, Bella, Antoni, Arnold).
- [ ] Select Adam → `character.voice_id` обновляется (Realtime push refreshes UI).
- [ ] Click `✎ adv` → input для custom voice_id → введи 20-char id → apply.
- [ ] Reject if id не 20 chars (alert или error chip).

## 9. Cost meter

- [ ] Stage 04 header показывает `💰 $X.XX` с `getProjectCostAction` sum.
- [ ] После каждого нового complete'а → meter refresh'ится (через Stage04 Provider jobs change).
- [ ] При cost ≥ `MANGO_COST_WARN_THRESHOLD_USD` ($10 default) → CostWarningToast снизу: «⚠ Потрачено $X.XX. Продолжить генерацию?» [Да] [Стоп].
- [ ] Click «Да, продолжить» → toast скрывается, sessionStorage flag set.
- [ ] Refresh страницы → flag сбрасывается, при следующем threshold breach toast снова появится.

## 10. Director rollback chat-tool

- [ ] Open Director Agent chat.
- [ ] Send: «откати кадр сцены 2 на предыдущую» → tool call `rollback_scene_version` с scene_id='s2', kind='first_frame', target_version_id отсутствует.
- [ ] Pending-card: «Откатить first_frame сцены s2?» с warning «Активная версия будет переключена на предыдущая версия. Downstream final_clip / master_clip станут stale.» [Подтвердить откат] [Отменить].
- [ ] Click «Подтвердить откат» → активная версия меняется → tool-chip в chat: «↺ Откат · сцена s2 · first_frame на пред.»
- [ ] Scene card обновляется auto: новая active version в preview, dot v_prev highlighted.
- [ ] Send: «откати кадр сцены 1 на v3» (если есть v3) → pending-card с target_version_id → confirm → version_id='v3' active.

## 11. Master clip versioning

- [ ] Все scene final_clips готовы → click `🎬 Финализировать ролик` в Stage 04 header → ffmpeg concat job submitted.
- [ ] On complete → `script.master_clip_versions.length === 1`, active = новый id.
- [ ] Change active video version в одной сцене (через rollback или regen) → final_clip в той сцене регенирится.
- [ ] Регенерация master_clip → versions.length = 2, новый = active.
- [ ] MasterClipModal открывается → видишь active master, download MP4 link.
- [ ] Counter `1/5` → `5/5` после 5 регенераций; 6-я выкидывает oldest.

## 12. Storage mirror (async)

- [ ] Generate first_frame → версия appended с `storage.kind='fal_passthrough'` (immediate).
- [ ] Через ~30 сек (после `mirrorSceneAssetToStorage` complete) → `storage.kind='supabase'`, `storage.bucket='scene-assets'`, `storage.path` matches expected pattern.
- [ ] Открой Supabase Storage UI → bucket `scene-assets` → файл `<user_id>/<project_id>/<scene_id>/<version_id>-frame.jpg` существует.
- [ ] Если 5/5 → 6th regen → dropped version: file в bucket удалён (best-effort).

## 13. Migration verification (legacy projects)

- [ ] Проект, созданный в Phase 1.3 (до 1.3.5 ship), загружается без ошибок.
- [ ] `script.scenes[0].first_frame_versions` содержит 1 elem (legacy `first_frame` обёрнут).
- [ ] `first_frame_active_version_id === first_frame_versions[0].version_id`.
- [ ] `audio_mode === 'auto'` (default applied).
- [ ] Если был legacy `master_clip` — `master_clip_versions.length === 1`, active = id.

## 14. Failure modes (graceful)

- [ ] fal API down (выключи FAL_KEY temporarily) → submit returns error chip, UI не crashes.
- [ ] Mirror fails (выключи Supabase service role key) → версия остаётся `fal_passthrough`, UI рендерит fal CDN URL.
- [ ] Migration script run twice → idempotent (skipped projects).
- [ ] Director вызывает rollback на проект без previous version → tool возвращает `ok:false` с понятным error.

## Sign-off

- [ ] Все items pass или formally deferred (P2 → 1.4).
- [ ] No tool-chip hallucinations (chat reports match jsonb state).
- [ ] No regression в Stage 02 character flow или Stage 03 script flow.
- [ ] Maintainer approved.

If all green → proceed to **Task 33 (apply prod migration + open PR + tag v1.3.5)**.
