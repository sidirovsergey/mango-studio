# Phase 1.8.0a — Script schema adapter Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Tasks use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Land `normalizeScript()` adapter as a pure refactor. Every script consumer reads through it. Generator/writers unchanged. Ships green with ZERO visible diff for the common case (description == description_ru); intentional one-time shift documented for the rare asymmetric case.

**Spec:** [`docs/superpowers/specs/2026-05-18-phase-1.8.0a-script-schema-adapter-design.md`](../specs/2026-05-18-phase-1.8.0a-script-schema-adapter-design.md)

**Pre-requisite:** v1.7.1 merged to main. Branch from `origin/main` AFTER v1.7.1 ships (clean rebase on Director Agent paths). Functionally independent — no DB or auth changes here.

---

## File Structure

### Created
- `packages/core/src/llm/normalize-script.ts` — `normalizeScript(raw): NormalizedScript`.
- `packages/core/src/llm/normalize-script.test.ts` — 14 unit tests per spec §5.
- `packages/core/src/llm/fixtures/legacy-symmetric.json` — description == description_ru.
- `packages/core/src/llm/fixtures/legacy-asymmetric.json` — description != description_ru.
- `packages/core/src/llm/fixtures/future-v1.8.json` — full new schema.
- `apps/web/src/hooks/useNormalizedScript.ts` — memoized hook keyed by `script` object identity.
- `apps/web/src/hooks/useNormalizedScript.test.ts`.
- `docs/audits/1.8.0a-stage-script-behavior-shift.md` — documents the one-time shift for asymmetric projects.

### Modified
- `packages/core/src/index.ts` — re-export `normalizeScript`, `NormalizedScene`, `NormalizedScript`, `ShotSpec`.
- `apps/web/src/components/workspace/stages/StageScript.tsx` — read `narrative_paragraph` (line ~337).
- `apps/web/src/components/workspace/stages/StageScript.test.tsx` — update snapshots.
- `apps/web/src/components/workspace/stages/scenes/SceneSidePanel.tsx` — iterate `dialogue[]` (line ~227).
- `apps/web/src/components/workspace/stages/scenes/SceneSidePanel.test.tsx`.
- `apps/web/src/components/workspace/Workspace.tsx` — wrap script with `useNormalizedScript`.
- `apps/web/src/server/actions/scripts.ts` — replace ad-hoc normalizer (~line 331) with `normalizeScript` call.
- `apps/web/src/server/actions/scripts.test.ts`.
- `packages/core/src/llm/director-state-summary.ts` — use `image_prompt_text` (line ~82), NOT `narrative_paragraph`.
- `packages/core/src/llm/director-state-summary.test.ts` — add English-context regression test.
- Any shot prompt builder in `packages/core/src/llm/` — switch to `image_prompt_text`.

---

## Sub-phase A — normalize-script module

### Task A1: Implement `normalizeScript`

**Files:**
- Create: `packages/core/src/llm/normalize-script.ts`

- [ ] **Step 1:** Copy implementation from spec §3 (with all Codex fixes: scene-level `.passthrough()`, nullable not default(''), two channels `narrative_paragraph` + `image_prompt_text`).
- [ ] **Step 2:** Export types: `NormalizedScene`, `NormalizedScript`, `ShotSpec`.
- [ ] **Step 3:** Commit `feat(core): normalizeScript adapter (Phase 1.8.0a A1)`.

### Task A2: Fixtures

**Files:**
- Create: `packages/core/src/llm/fixtures/legacy-symmetric.json`
- Create: `packages/core/src/llm/fixtures/legacy-asymmetric.json`
- Create: `packages/core/src/llm/fixtures/future-v1.8.json`

- [ ] **legacy-symmetric:** 4 scenes, `description == description_ru` (Russian), `description_en` English, `dialogue: { speaker, text }` or null.
- [ ] **legacy-asymmetric:** same shape but `description` is English, `description_ru` Russian — different text bodies.
- [ ] **future-v1.8:** scenes with `narrative_paragraph`, `dialogue: [a, b]`, `shots: [s1, s2]`, no `description` field at all.
- [ ] Commit `test(core): normalize-script fixtures (Phase 1.8.0a A2)`.

### Task A3: 14 unit tests

**Files:**
- Create: `packages/core/src/llm/normalize-script.test.ts`

- [ ] Implement tests 1-14 from spec §5 table. Pay extra attention to:
  - Test 11 (asymmetric): `narrative_paragraph === description_ru` AND `image_prompt_text === description_en`.
  - Test 12 (director state preserves English): build summary fixture + verify output substring matches `description_en` source.
  - Test 13 (scene .passthrough): add `my_custom_field: 'x'` to fixture scene → assert preserved in parsed output.
  - Test 14 (raw reference): `result.raw === inputObject` (===, not deep).
- [ ] All 14 green.
- [ ] Commit `test(core): normalize-script 14 unit tests (Phase 1.8.0a A3)`.

---

## Sub-phase B — Workspace hook + memo

### Task B1: `useNormalizedScript` hook

**Files:**
- Create: `apps/web/src/hooks/useNormalizedScript.ts`
- Create: `apps/web/src/hooks/useNormalizedScript.test.ts`

- [ ] **Step 1:** Hook signature: `useNormalizedScript(script: unknown | null): NormalizedScript | null`.
- [ ] **Step 2:** Memoize via `useMemo(() => script ? normalizeScript(script) : null, [script])` — depends on script object identity.
- [ ] **Step 3:** Tests: same identity → same normalized reference; different identity → new normalization.
- [ ] **Step 4:** Commit `feat(web): useNormalizedScript hook (Phase 1.8.0a B1)`.

### Task B2: Wire `Workspace.tsx`

**Files:**
- Modify: `apps/web/src/components/workspace/Workspace.tsx`

- [ ] **Step 1:** At workspace boundary: `const normalized = useNormalizedScript(project.script)`.
- [ ] **Step 2:** Pass `normalized` down to StageScript / Stage 04 / etc via existing prop/context channels. Adjust types.
- [ ] **Step 3:** TypeScript: ensure types compile. Existing components currently take `script: ScriptOutput` — change to `normalized: NormalizedScript`.
- [ ] **Step 4:** Commit `refactor(workspace): pass NormalizedScript through (Phase 1.8.0a B2)`.

---

## Sub-phase C — Stage 03 + Stage 04 readers

### Task C1: `StageScript.tsx` refactor

**Files:**
- Modify: `apps/web/src/components/workspace/stages/StageScript.tsx`
- Modify: `apps/web/src/components/workspace/stages/StageScript.test.tsx`

- [ ] **Step 1:** Change `scene.description` → `scene.narrative_paragraph` (around line 337 per Codex citation).
- [ ] **Step 2:** Update test snapshots: re-run, accept new snapshots ONLY where the symmetric fixture (legacy-symmetric.json) produces IDENTICAL rendered HTML to pre-refactor. If symmetric case diffs → adapter logic wrong; debug.
- [ ] **Step 3:** Add new test with legacy-asymmetric fixture: assert rendered text contains `description_ru` body, NOT `description` body. Document this as the intentional shift.
- [ ] **Step 4:** Commit `refactor(stage-script): read narrative_paragraph (Phase 1.8.0a C1)`.

### Task C2: `SceneSidePanel.tsx` refactor (dialogue list)

**Files:**
- Modify: `apps/web/src/components/workspace/stages/scenes/SceneSidePanel.tsx`
- Modify: `apps/web/src/components/workspace/stages/scenes/SceneSidePanel.test.tsx`

- [ ] **Step 1:** Replace `scene.dialogue?.text` (line ~227) with iteration: `scene.dialogue.map(d => <p key={...}>{d.speaker}: {d.text}</p>)`.
- [ ] **Step 2:** Handle three cases:
  - Empty array (legacy null) → render nothing OR placeholder «(нет реплик)».
  - Single → renders one line.
  - Multi (future) → renders all lines, each as `<p>speaker: text</p>`.
- [ ] **Step 3:** Tests for all three.
- [ ] **Step 4:** Commit `refactor(scene-panel): iterate dialogue[] (Phase 1.8.0a C2)`.

### Task C3: Stage 04 inline refactor

**Files:**
- Modify: any other Stage 04 component reading `scene.description` → `scene.narrative_paragraph`.

- [ ] Grep `grep -rn "scene\.description[^_]" apps/web/src/components/workspace/stages/scenes/` — list remaining occurrences.
- [ ] Replace with `scene.narrative_paragraph` per file.
- [ ] Snapshot tests re-baselined (symmetric fixture ZERO diff; asymmetric documented).
- [ ] Commit `refactor(stage-04): narrative_paragraph in inline cards (Phase 1.8.0a C3)`.

---

## Sub-phase D — Server actions

### Task D1: `scripts.ts` ad-hoc normalizer removal

**Files:**
- Modify: `apps/web/src/server/actions/scripts.ts`
- Modify: `apps/web/src/server/actions/scripts.test.ts`

- [ ] **Step 1:** Locate ad-hoc legacy migration logic at ~line 331.
- [ ] **Step 2:** Replace with `const normalized = normalizeScript(rawScript)`; use `normalized.scenes` etc downstream.
- [ ] **Step 3:** If the ad-hoc helper is no longer used elsewhere → DELETE.
- [ ] **Step 4:** Tests re-run; semantic preservation.
- [ ] **Step 5:** Commit `refactor(actions): scripts.ts uses normalizeScript (Phase 1.8.0a D1)`.

### Task D2: `regenSceneTextAction` (LLM tool surface freeze)

**Files:**
- Modify: `apps/web/src/server/actions/regenSceneTextAction.ts`

- [ ] **Step 1:** Internal reads switch from `scene.description` → `scene.narrative_paragraph`.
- [ ] **Step 2:** Writes UNCHANGED — keep writing to `description_ru` + `description` (legacy fields). DO NOT introduce `narrative_paragraph` as a writeable column.
- [ ] **Step 3:** No test changes needed (behavioral identity for symmetric data).
- [ ] **Step 4:** Commit `refactor(regen-scene): internal narrative_paragraph read (Phase 1.8.0a D2)`.

---

## Sub-phase E — Director Agent state summary

### Task E1: `director-state-summary.ts` use `image_prompt_text`

**Files:**
- Modify: `packages/core/src/llm/director-state-summary.ts`
- Modify: `packages/core/src/llm/director-state-summary.test.ts`

- [ ] **Step 1:** Line ~82 `const rawDesc = (scene.description_en ?? null) || scene.description;` → `const rawDesc = scene.image_prompt_text;` (adapter has already applied the same default chain).
- [ ] **Step 2:** Add regression test (spec §5 test #12 equivalent at this layer): fixture with `description_en="English visual prompt"` + `description_ru="Русский текст"` → director-state-summary output contains "English visual prompt", NOT "Русский текст".
- [ ] **Step 3:** Test passes — confirms language flip caught by Codex did NOT happen.
- [ ] **Step 4:** Commit `fix(director-summary): use image_prompt_text to preserve English context (Phase 1.8.0a E1)`.

### Task E2: Shot prompt builders

**Files:**
- Modify: any file in `packages/core/src/llm/` building image/video prompts that reads `scene.description_en` or `scene.description`.

- [ ] Grep `grep -rn "description_en" packages/core/src/llm/`. Replace with `image_prompt_text` where the consumer is a prompt builder feeding image/video models.
- [ ] Leave `description_en` reads UNTOUCHED in writers and storage layer.
- [ ] Commit per file or as one tidy commit `refactor(llm-prompts): image_prompt_text for downstream models (Phase 1.8.0a E2)`.

---

## Sub-phase F — LLM tool surface grep gate

### Task F1: Verify no `narrative_paragraph` leak into LLM-facing tool schemas

**Files:**
- Inspect: `packages/core/src/llm/director-tools.ts`
- Inspect: any tool inputSchema or tool description string in `apps/web/src/server/actions/chat.ts`, `apps/web/src/server/lib/director-tools.ts`, etc.

- [ ] `rg "narrative_paragraph" packages/core/src/llm/director-tools.ts` → expect ZERO matches.
- [ ] `rg "narrative_paragraph" apps/web/src/server/actions/` → zero matches in LLM-facing files.
- [ ] `rg "narrative_paragraph" apps/web/src/server/lib/director-tools.ts` → zero matches.
- [ ] If ANY match → revert that file's LLM-facing description to legacy `description` and use internal `narrative_paragraph` only in non-LLM-facing code paths.
- [ ] No commit if grep clean — just confirm in PR description.

---

## Sub-phase G — Verification + rollout

### Task G1: Full suite green

- [ ] `pnpm turbo test` — all tests pass including 14 normalize-script unit tests + asymmetric snapshot test + director-state-summary regression.
- [ ] `pnpm turbo lint` — biome clean. **Explicit reminder: subagents skip biome by default.**
- [ ] `pnpm turbo typecheck` — 0 TS errors.
- [ ] Manual prod-data spot-check: `mcp__supabase__execute_sql(query="SELECT id, COUNT(*) FILTER (WHERE script->'scenes'->0->>'description' IS DISTINCT FROM script->'scenes'->0->>'description_ru') AS asymmetric_count FROM projects GROUP BY 1 LIMIT 10")` — confirm zero or near-zero asymmetric projects in prod. Document count in audit doc.

### Task G2: Behavior-shift audit doc

**Files:**
- Create: `docs/audits/1.8.0a-stage-script-behavior-shift.md`

- [ ] Document:
  - The shift: for projects where `description != description_ru`, Stage 03 text now reads `description_ru` (was reading `description`).
  - Why: Russian-canonical UI per CJM § 7 (Russian audience).
  - Prod impact: spot-check result from G1 step 4.
  - Manual remediation if any: for affected projects, operator can copy `description` → `description_ru` via one-off SQL if user complains.
- [ ] Commit `docs(audit): 1.8.0a behavior shift for asymmetric projects (Phase 1.8.0a G2)`.

### Task G3: Codex pre-PR audit

- [ ] Codex task focused on: adapter correctness for asymmetric cases, director-state-summary English preservation, LLM tool surface freeze grep gate, memoization correctness.
- [ ] Apply any fixes; re-run tests.

### Task G4: Open PR + merge + tag

- [ ] `gh pr create` — body references spec + adapter test count + behavior shift audit.
- [ ] Vercel preview smoke: open one project in Stage 03 and Stage 04 — verify text renders correctly. Open Director Agent chat — verify LLM tool calls still work and don't claim to update `narrative_paragraph` (they should still say «обновил description» per legacy surface).
- [ ] Squash-merge to main → Vercel deploys.
- [ ] Prod smoke: hit `/workspace/[id]` for one Russian project → verify Stage 03 + 04 render correctly.
- [ ] `git tag v1.8.0a && git push --tags`.
- [ ] Update memory `project_phase1.8.0a_status.md`.

---

## Done criteria

- [ ] `normalizeScript` exported from `@mango/core` with 14 passing tests.
- [ ] All script consumers route through adapter (verified by `rg "scene\.description[^_]" apps/web/src/components/workspace/` returning zero matches in render paths).
- [ ] Director Agent tool schemas unchanged (grep gate clean).
- [ ] `director-state-summary` produces ENGLISH context for projects with `description_en` set (regression test gate).
- [ ] Symmetric-fixture snapshot tests show ZERO diff vs pre-1.8.0a baseline.
- [ ] Asymmetric-fixture snapshot tests show INTENTIONAL shift, documented.
- [ ] No DB migration touched.
- [ ] Codex audit passed.
