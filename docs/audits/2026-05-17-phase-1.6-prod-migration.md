# Phase 1.6 — Prod migration + owner pre-seed

**Date applied:** 2026-05-17 08:30 UTC
**Migration:** `20260518000001_user_accounts.sql` → Supabase tracked as `20260517083006/20260518000001_user_accounts`
**Project:** `mgsfjyojbidhkxiknhsy`
**Applied via:** `mcp__supabase__apply_migration` from controller session.

## Backfill result

```
tier  | count
------|------
trial | 65
```

After pre-seed (next section):

```
tier   | count
-------|------
free   | 1
trial  | 64
```

## Triggers verified

```
tgname                                            | on_table        | tgenabled
--------------------------------------------------|-----------------|---------
tg_user_accounts_provision_on_auth_users_insert   | auth.users      | O
tg_user_accounts_upgrade_on_email_identity_insert | auth.identities | O
```

Both `SECURITY DEFINER` triggers active.

## Owner pre-seed

Top owner by project count (assumed = Sergey / operator):

```
user_id                              | project_count
-------------------------------------|--------------
5593e0ca-15d0-4f65-be27-f9f7e592e185 | 8
b5989c2a-4d1c-4bce-b90d-9293a3e66c5c | 5
```

Pre-seeded `5593e0ca-15d0-4f65-be27-f9f7e592e185` → `tier='free'`, `upgraded_to_free_at='2026-05-17 08:30:53.732348+00'`.

If this UUID is not the operator's account, reverse with:
```sql
update public.user_accounts
set tier='trial', upgraded_to_free_at=null
where user_id='5593e0ca-15d0-4f65-be27-f9f7e592e185';
```
And re-run the top-by-project-count query against the correct account.

## Independent prod verification (post-Codex audit)

Codex sub-phase A+B audit (2026-05-17) flagged: prod verification should be
recorded with raw rows, not just doc claims. Below are the raw rows pulled
via `mcp__supabase__execute_sql` against project `mgsfjyojbidhkxiknhsy`
**after** the migration was applied.

**RLS policies (`pg_policy`):**

```
polname                   | polcmd | using_expr
--------------------------|--------|-----------------------
user_accounts_own_select  | r      | (user_id = auth.uid())
user_accounts_own_update  | w      | (user_id = auth.uid())
```

INSERT and DELETE are intentionally absent: the two SECURITY DEFINER
triggers do all writes; `service_role` bypasses RLS for backfill /
admin operations.

**Trigger function config (`pg_proc`):**

```
proname                                    | prosecdef | proconfig
-------------------------------------------|-----------|----------------------------------------
tg_user_accounts_provision                 | true      | {"search_path=\"\", pg_catalog, public"}
tg_user_accounts_upgrade_on_email_identity | true      | {"search_path=\"\", pg_catalog, public"}
```

`SECURITY DEFINER` confirmed (`prosecdef=true`). `search_path` is explicit:
empty user schema first (defense-in-depth against schema-injection), then
`pg_catalog`, then `public`. All table references inside the function
bodies are schema-qualified (`public.user_accounts`, `auth.users` via
trigger `NEW` row), so resolution is unambiguous.

**Trigger registration (`pg_trigger`):**

```
tgname                                            | on_table        | tgenabled
--------------------------------------------------|-----------------|----------
tg_user_accounts_provision_on_auth_users_insert   | auth.users      | O
tg_user_accounts_upgrade_on_email_identity_insert | auth.identities | O
```

`tgenabled='O'` = enabled in origin mode (fires for normal writes and on
replicas).

## Follow-ups (not blocking)

- **NTH (Codex)** — strengthen `MediaJobKind` invariant test: replace the
  length check with a derived const tuple (`MEDIA_JOB_KINDS as const` →
  `type MediaJobKind = typeof MEDIA_JOB_KINDS[number]`) so adding a literal
  to the union without DB backing fails compilation. Deferred — affects
  call sites; revisit when touching scene-helpers next.
