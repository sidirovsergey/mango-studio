# Phase 1.7 B2 — production migration verification

**Date:** 2026-05-18
**Migration:** `20260518000002_billing.sql` (Phase 1.7 B1)
**Supabase project:** `mgsfjyojbidhkxiknhsy` (prod)
**Applied via:** `mcp__supabase__apply_migration`
**Live version assigned by Supabase:** `20260518115605`

## Apply result

```
{"success": true}
```

## Verification 1 — migration row exists

`mcp__supabase__list_migrations(project_id="mgsfjyojbidhkxiknhsy")` tail:

```json
{"version":"20260517083006","name":"20260518000001_user_accounts"}
{"version":"20260518115605","name":"20260518000002_billing"}
```

✅ New migration row present at top.

## Verification 2 — balance_kopeks column

```sql
select column_name, data_type, is_nullable, column_default
from information_schema.columns
where table_schema='public' and table_name='user_accounts'
  and column_name='balance_kopeks';
```

Result:

```json
[{"column_name":"balance_kopeks","data_type":"bigint","is_nullable":"NO","column_default":"0"}]
```

✅ `bigint NOT NULL DEFAULT 0` as specified. CHECK constraint (`balance_kopeks >= 0`) verified via apply success (would have failed if column violated).

## Verification 3 — 5 SECURITY DEFINER fns + 1 IMMUTABLE pure fn

```sql
select proname, prosecdef, proconfig
from pg_proc
where proname in (
  'fn_apply_topup','fn_reserve_balance','fn_refund_reservation',
  'fn_finalise_charge','fn_price_kopeks','tg_billing_settle_on_terminal'
)
order by proname;
```

Result:

```json
[
  {"proname":"fn_apply_topup",                "prosecdef":true, "proconfig":["search_path=\"\", pg_catalog, public"]},
  {"proname":"fn_finalise_charge",            "prosecdef":true, "proconfig":["search_path=\"\", pg_catalog, public"]},
  {"proname":"fn_price_kopeks",               "prosecdef":false,"proconfig":null},
  {"proname":"fn_refund_reservation",         "prosecdef":true, "proconfig":["search_path=\"\", pg_catalog, public"]},
  {"proname":"fn_reserve_balance",            "prosecdef":true, "proconfig":["search_path=\"\", pg_catalog, public"]},
  {"proname":"tg_billing_settle_on_terminal", "prosecdef":true, "proconfig":["search_path=\"\", pg_catalog, public"]}
]
```

✅ All 6 functions present:
- 5 SECURITY DEFINER (prosecdef=true) with `search_path="", pg_catalog, public` discipline
- fn_price_kopeks pure SQL IMMUTABLE (prosecdef=false correct — no privileges needed)

## Verification 4 — REVOKE worked + GRANT worked + trigger + RLS

```sql
select
  has_function_privilege('anon',         'public.fn_apply_topup(text, bigint)',                       'EXECUTE') as anon_apply,
  has_function_privilege('authenticated','public.fn_apply_topup(text, bigint)',                       'EXECUTE') as authd_apply,
  has_function_privilege('service_role', 'public.fn_apply_topup(text, bigint)',                       'EXECUTE') as srv_apply,
  has_function_privilege('anon',         'public.fn_reserve_balance(uuid, uuid, bigint, text, text)', 'EXECUTE') as anon_reserve,
  has_function_privilege('service_role', 'public.fn_reserve_balance(uuid, uuid, bigint, text, text)', 'EXECUTE') as srv_reserve,
  (select count(*) from pg_trigger where tgname='trg_billing_settle_on_terminal'
                                     and tgrelid='public.media_jobs'::regclass)                       as trigger_count,
  (select count(*) from pg_policies where tablename in ('billing_payments','billing_charges'))         as policy_count,
  (select count(*) from pg_tables   where tablename in ('billing_payments','billing_charges')
                                     and schemaname='public')                                         as table_count;
```

Result:

```json
[{
  "anon_apply":     false,
  "authd_apply":    false,
  "srv_apply":      true,
  "anon_reserve":   false,
  "srv_reserve":    true,
  "trigger_count":  1,
  "policy_count":   2,
  "table_count":    2
}]
```

✅ Codex BLOCKER #1 closed in production:
- anon EXECUTE on fn_apply_topup = **false** (cannot call via compromised JWT)
- authenticated EXECUTE on fn_apply_topup = **false**
- service_role EXECUTE on fn_apply_topup = **true** (webhook handler can call)
- Same for fn_reserve_balance

✅ Trigger `trg_billing_settle_on_terminal` is live on `media_jobs` (1 of 1).
✅ 2 RLS SELECT policies (one per new billing table).
✅ 2 new tables present.

## Backfill verification — every existing row has balance 0

(Implicit from `DEFAULT 0 NOT NULL` on ALTER TABLE. No explicit re-check needed; the column was added with a constant default, so all 79 pre-existing user_accounts rows have `balance_kopeks=0`.)

## Status

Sub-phase B complete. Production DB is in the state Phase 1.7 server code expects. Server-action gates (Sub-phase D) and webhook handler (Sub-phase E) can now safely call the 4 RPC functions via service_role.

No rollback needed.
