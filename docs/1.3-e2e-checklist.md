# Phase 1.3 — Manual E2E checklist

**Status:** awaiting live session with maintainer.

Per [feedback_e2e_with_user_before_merge](memory:feedback_e2e_with_user_before_merge) rule, this fast must complete BEFORE merge. Static checks (typecheck + tests) cannot detect Director chat-tool hallucinations or polling/Realtime regressions; only a live walkthrough can.

## Pre-flight

- [ ] Branch `claude/phase-1.3` checked out locally (or pulled to a deploy preview).
- [ ] Migrations applied to staging Supabase (already done — `media_jobs_unify`, `scene_assets_bucket`).
- [ ] ENV variables in local `.env.local`:
  - [ ] `FAL_KEY` — real key, NOT placeholder
  - [ ] `OPENROUTER_API_KEY` — real
  - [ ] `MANGO_DEFAULT_NARRATOR_VOICE_ID` (optional override; defaults to ElevenLabs Rachel `21m00Tcm4TlvDq8ikWAM`)
  - [ ] `MEDIA_PROVIDER=fal`, `LLM_PROVIDER=openrouter`
  - [ ] `STORAGE_PROVIDER=fal_cdn` (or `supabase` if testing user uploads end-to-end)
- [ ] `pnpm dev` starts without errors.

## Stage 02 character flow (regression check — Task 12 risk)

- [ ] Create new project «дельфин ищет работу».
- [ ] Stage 02: trigger character dossier generation. **Confirm UI eventually shows the image** (polling now drives this — was sync in 1.2). May take 30-60s.
- [ ] If image never appears → polling/realtime broken; check `pollMediaJobsAction` + `subscribeMediaJobs` logs.

## Stage 03 script generation

- [ ] Generate script.
- [ ] In dev tools, inspect `projects.script` jsonb. Expect each scene to have:
  - [ ] `dialogue` (object or null) — NOT `voiceover` string
  - [ ] `character_ids` — array
  - [ ] `first_frame_source: 'auto_continuity'`
  - [ ] all asset slots (`first_frame`, `last_frame`, `video`, `voice_audio`, `final_clip`) = null
  - [ ] root has `narrator_voice` and `master_clip: null`
- [ ] Open existing legacy project (created in 1.2 with `voiceover: string`). Stage 04 should still render via `normalizeScene`.

## Stage 04 — Storyboard

- [ ] Page loads. Counters show `0/N first frames`, `0/N videos`, `0/N final`.
- [ ] Click **📷 Generate First Frames** (top bulk CTA). Up to 5 spinners appear in parallel. After ~30-60s, images fill in.
- [ ] Cap edge case: if scene count > 5, header should say "capped" or similar.
- [ ] Click **V** (regen video) on scene #1. Spinner ~60-180s. Video player appears inline when done.
- [ ] After scene #1's video lands, scene #2's first_frame shows 🔁 stale badge (cascade).
- [ ] Click **I** on scene #2 → re-generates with continuity ref from scene #1's last_frame.

## Audio-aware branching

- [ ] Switch project tier to **Премиум** (TopBar toggle). Scene cards default to `bytedance/seedance-2.0/image-to-video` (🎵 native audio badge).
- [ ] Generate video for scene #3 → final_clip = video automatically (no TTS+mux job).
- [ ] Switch a single scene to silent model via per-card model selector (e.g., `fal-ai/kling-video/v2.5-turbo/pro/image-to-video`). Badge changes to 🔇.
- [ ] Generate video → voice TTS → mux → final_clip cascade fires.

## Master compilation

- [ ] When all scenes have `final_clip`, **🎬 Финализировать ролик** CTA enables.
- [ ] Click → master_clip job submits → ~10-30s → **MasterClipModal** opens with autoplay 9:16 player + Download MP4 link.
- [ ] Regenerate one scene's video. Reload page. Master should show 🔁 stale badge in modal.

## Director chat-tools (1.2.5 hallucination risk)

Test EVERY tool — each must show its tool-chip in chat AND its actual server-side effect. **Critical anti-pattern:** chat says "готово!" but the actual scene didn't change. If any tool shows that gap, hold the merge.

- [ ] Chat: «перегенери видео сцены 3» → pending-card "Перегенерировать видео сцены s3?" → Confirm → tool-chip ✅ → `media_jobs` row exists for kind=`video` scene_id=`s3`.
- [ ] Chat: «обнови реплику сцены 2 в смешном тоне» → immediate execute → chip "✏️ Обновил описание сцены" → `script.scenes[1].dialogue.text` actually changed.
- [ ] Chat: «сделай сцену 4 длиной 10 секунд» → immediate execute → chip "Длительность сцены установлена в 10s" (or similar) → `script.scenes[3].duration_sec === 10`.
- [ ] Chat: «используй Veo 3.1 для сцены 5» → pending-card "Сменить модель сцены s5 на veo3.1/image-to-video?" → Confirm → `scene.config_overrides.model` set.
- [ ] Chat: «сгенерируй первый кадр сцены 1» → immediate execute → chip "🖼️ Сгенерил first frame сцены s1" → media_jobs row.
- [ ] Chat: «финализируй ролик» → pending-card "Финализировать ролик?" → Confirm → master_clip job submits.

## Manual upload + cancel + retry

- [ ] Click **⬆ image** on scene #5, pick a local PNG. After upload, scene shows the user image; `first_frame_source = 'user_upload'`.
- [ ] Start a video gen → click **✕** in spinner area → media_jobs row removed; fal job cancelled (verify in fal.ai dashboard if doable).
- [ ] Force a job to error (e.g., disconnect FAL_KEY temporarily before submit). After error appears in UI → click retry button → new pending job submits with same request_input.

## Tab close / reconcile

- [ ] Start 3 video gens → close browser tab → wait 90s → reopen → page should show 3 completed videos (poll-on-mount catches up).

## Sign-off

- [ ] Maintainer signed off on each box above.
- [ ] No tool-chip hallucinations observed (chat reports match actual jsonb state).
- [ ] No Stage 02 regression (character images appear via polling).
- [ ] All blocking issues filed and either fixed or formally deferred to 1.3.5/1.4.

If all green → proceed to **Task 45 (Open PR)** then **Task 46 (Merge + tag)**.
